/**
 * Argument resolution SPI for converting method parameters into values suitable
 * for use in translation.
 * <p>
 * Implementations convert invocation arguments into ordered components that can
 * be referenced by translation keys.
 * </p>
 */
package io.github.namiuni.doburoku.common.argument;
