/**
 * Result resolution SPI for producing a final value from a translation key and
 * its rendered arguments.
 * <p>
 * Implementations may return different result types depending on the target API.
 * </p>
 */
package io.github.namiuni.doburoku.common.result;
